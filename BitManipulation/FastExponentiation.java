public class FastExponentiation {
    public static void main(String[] args) {
        // logic is that binaries are log n + 1 hence
        // run loop as per power but in binary

        fastExponen(num, pow);
    }

    public static int fastExponen(int num, int pow) {

    }
}
